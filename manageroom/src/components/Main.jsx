import React from 'react';              //Reactを読み込んでいる

const Main = (props) =>{
    const {data} = props;
    return(
        <div>
            alert({roomId});
        </div>
    )
}
 
export default Main;